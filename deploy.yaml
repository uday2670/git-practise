apiversion: app/v1
 kind: deployment
 metadata: 
   name: nginx-deployment
   app: nginx
    spec:
    selector:
      matchLabel:
        app: nginx
      replicas: 3
      stratagy:
        type: RollingUpdate
